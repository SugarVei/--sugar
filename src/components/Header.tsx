import { Grid2X2, List, Moon, Plus, Search, Sun, X } from 'lucide-react'
import { BrandMark } from './BrandMark'
import type { ViewMode } from '../types/project'

interface Props { query: string; setQuery: (query: string) => void; view: ViewMode; setView: (view: ViewMode) => void; theme: 'light' | 'dark'; toggleTheme: () => void; onCreate: () => void }
export function Header({ query, setQuery, view, setView, theme, toggleTheme, onCreate }: Props) { return <header className="site-header"><div className="header-inner"><div className="brand"><BrandMark /><div><strong>Sugar Project Hub</strong><span>个人项目可视化管理</span></div></div><label className="search-field"><Search size={18} /><span className="sr-only">搜索项目</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="搜索项目、分类或关键词…" />{query && <button type="button" onClick={() => setQuery('')} aria-label="清除搜索"><X size={16} /></button>}</label><div className="header-actions"><div className="segmented" aria-label="切换项目视图"><button className={view === 'grid' ? 'selected' : ''} type="button" onClick={() => setView('grid')} aria-label="卡片视图"><Grid2X2 size={17} /></button><button className={view === 'list' ? 'selected' : ''} type="button" onClick={() => setView('list')} aria-label="列表视图"><List size={18} /></button></div><button type="button" className="icon-button" onClick={toggleTheme} aria-label="切换主题">{theme === 'light' ? <Moon size={18} /> : <Sun size={18} />}</button><button type="button" className="primary-button create-button" onClick={onCreate}><Plus size={18} />新建项目</button></div></div></header> }
