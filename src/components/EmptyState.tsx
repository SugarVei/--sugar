import { FolderOpen, SearchX } from 'lucide-react'
export function EmptyState({ searching, onClear, onCreate }: { searching: boolean; onClear?: () => void; onCreate?: () => void }) { return <div className="empty-state">{searching ? <SearchX size={30} /> : <FolderOpen size={31} />}<h2>{searching ? '没有匹配的项目' : '从一个项目开始吧'}</h2><p>{searching ? '试试更宽泛的关键词，或清除当前筛选条件。' : '把你的网页、工具与研究成果收进这个安静的工作台。'}</p>{searching ? <button type="button" className="secondary-button" onClick={onClear}>清除全部筛选</button> : <button type="button" className="primary-button" onClick={onCreate}>新建第一个项目</button>}</div> }
